{
  "name": "aervinex-functions",
  "description": "AERVINEX Cloud Functions (Node.js 18) — sensor ingest, ML risk compute, FCM alerts, daily/realtime aggregation, GDPR export, webhook dispatch, BQ export, ML drift detector.",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "18"
  },
  "main": "index.js",
  "scripts": {
    "lint": "eslint .",
    "serve": "firebase emulators:start --only functions",
    "shell": "firebase functions:shell",
    "start": "npm run shell",
    "deploy": "firebase deploy --only functions:nodejs",
    "logs": "firebase functions:log"
  },
  "dependencies": {
    "firebase-admin": "^12.1.0",
    "firebase-functions": "^4.9.0",
    "@google-cloud/bigquery": "^7.7.0",
    "@google-cloud/storage": "^7.10.0",
    "node-fetch": "^2.7.0",
    "midtrans-client": "^1.3.1"
  },
  "devDependencies": {
    "eslint": "^8.57.0"
  }
}

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
firebase-debug.log*
firebase-debug.*.log*

# Firebase cache
.firebase/

# Firebase config

# Uncomment this if you'd like others to create their own Firebase project.
# For a team working on the same Firebase project(s), it is recommended to leave
# it commented so all members can deploy to the same project(s) in .firebaserc.
# .firebaserc

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Directory for instrumented libs generated by jscoverage/JSCover
lib-cov

# Coverage directory used by tools like istanbul
coverage

# nyc test coverage
.nyc_output

# Grunt intermediate storage (http://gruntjs.com/creating-plugins#storing-task-files)
.grunt

# Bower dependency directory (https://bower.io/)
bower_components

# node-waf configuration
.lock-wscript

# Compiled binary addons (http://nodejs.org/api/addons.html)
build/Release

# Dependency directories
node_modules/

# Optional npm cache directory
.npm

# Optional eslint cache
.eslintcache

# Optional REPL history
.node_repl_history

# Output of 'npm pack'
*.tgz

# Yarn Integrity file
.yarn-integrity

# dotenv environment variables file
.env

# Firebase service accounts & credentials
.secrets/
*-sa.json
*service-account*.json
serviceAccountKey.json

# Firmware build artifacts + secrets
firmware/build/
firmware/**/secrets.h
firmware/**/*.bin

# Python ML training environment
ml/venv/
ml/__pycache__/
ml/**/*.pyc
ml/training/venv/
ml/training/__pycache__/
ml/models/*.onnx
ml/training/data/

# Local test outputs
ml/local-test/results.json
ml_output/

# Raw training datasets (too large for git — fetch via PhysioNet / Kaggle scripts)
data/
*.csv
!ml/local-test/*.csv
!firmware/library_dependencies.txt

# Large binary outputs
*.bin
*.zip
*.tar.gz

# Trained ML model weights (use Hugging Face / Firebase Storage instead)
*.pkl
*.joblib
*.h5
*.pb
*.pt
*.pth
*.tflite
!ml/models/version.json
public/models/*.onnx
ml/models/*.onnx
functions/ml_models/

# OS / IDE
.DS_Store
Thumbs.db
.vscode/settings.json
.idea/

# Claude / agent transcripts
.claude/

# Backup files
*.bak
*~

